#include<stdio.h>
#include<math.h>

int main(){
    
    float valor = (-(-9) + sqrt((-9) * (-9) - 4*3*6)/(2*3));
    printf("letra a) %f", valor);
    
    int arg = ((pow(3, 2) == 9) && (acos(0) == 0)) || (4 % 8 == 3);
    printf("letra b) %d", arg); 
    
    return 0;
}
